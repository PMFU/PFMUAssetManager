#include "AssetManager.h"

#include <fstream>
#include <filesystem>

void AssetManager::build()
{
	//Do Asset Discovery

	//Iterate through the file extension types, and use job system to async load the stuff
}
